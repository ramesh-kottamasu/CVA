# Base URL of the deployed backend (no trailing slash).
# Leave empty in local dev — the Vite proxy handles /api → localhost:8000.
# In production (Vercel) set this to your Render / Railway backend URL.
VITE_API_URL=https://your-backend.onrender.com
